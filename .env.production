# Production Environment Variables (Template)
# Copy this file and fill in actual production values

NODE_ENV=production

# Database (Use managed PostgreSQL service in production)
DATABASE_URL=postgresql://user:password@host:port/database

# Redis (Use managed Redis service in production)
REDIS_URL=redis://host:port

# JWT (Generate secure random keys for production)
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_KEY_IN_PRODUCTION
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# Email (Use a proper SMTP service like SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
EMAIL_FROM=noreply@yourdomain.com

# File Upload
MAX_FILE_SIZE=10MB
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp
UPLOAD_DIR=./uploads

# OCR
OCR_LANGUAGE=eng
OCR_CONFIDENCE_THRESHOLD=60

# Product APIs
UPC_API_KEY=your-upc-api-key

# Notifications
NOTIFICATION_REMINDER_DAYS=30,7,1

# Security
BCRYPT_ROUNDS=12

# CORS (Set to your actual domain)
CORS_ORIGINS=https://yourdomain.com

# Logging
LOG_LEVEL=warn

# Frontend URLs
VITE_API_URL=https://api.yourdomain.com
VITE_APP_NAME=Receipt Tracker
VITE_APP_VERSION=1.0.0
